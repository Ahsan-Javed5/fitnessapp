/// I will be using this class to create 3 modes of theme
/// for my custom widgets
/// For example we will pass small to CustomTextButton constructor
/// instead of padding, fontSize and other config values to reduce boilerplate code
enum ThemeSize { large, medium, small }