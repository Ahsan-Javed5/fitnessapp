class CoachCollection {
  final String name;
  final String image;
  final int count;
  final String routeName;
  final int pageIndex;
  final dynamic data;

  CoachCollection(this.name, this.image, this.count, this.routeName,
      this.pageIndex, this.data);
}
